import StayWithMe from "./components/StayWithMe";

/**
 * App.tsx
 * Renders the Stay With Me full-screen overlay as the root view.
 * The component manages its own state transitions internally.
 */
export default function App() {
  return <StayWithMe />;
}
